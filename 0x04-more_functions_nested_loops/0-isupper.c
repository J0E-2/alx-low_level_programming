#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: character whose case is being checked
 *
 * Return: 1 || 0
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90 )
return (1);
else
return (0);
}
