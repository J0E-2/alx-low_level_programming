#include "main.h"
/**
 *
 *
 *
 */
void print_diagonal(int n)
{
  int i;
  int j;
  i = (' ');
  i = 0;
  j = 1;
    while (j <= n)
      {
       if (n <= 0)
	_putchar('\n');
      else
	{
       _putchar(i);
  _putchar(92);
	}
       _putchar('\n');
  i++;
  j++;
      }
}
