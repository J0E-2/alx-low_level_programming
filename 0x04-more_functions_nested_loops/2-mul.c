#include "main.h"
/**
 * mul - multiplies two integers
 * @a: integer1 to be multiplied
 * @b: integer2 to be multiplied
 *
 * Return: result of multiplication
 */
int mul(int a, int b)
{
int i;
i = (a * b);
return (i);
}
