#include "main.h"
/**
 * _islower - function checks for lowercase characters
 * @c: parameter
 * Return: on success 1 // 0
 */
int islower(int c);
int _islowercase(int c)
{
c = 0;
while(c <= 122)
{
_islower(c);
if (_islower(c) = !0)
return (1);
else
return (0);
}
}
