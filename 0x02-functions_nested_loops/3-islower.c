#include "main.h"
/**
 * _islower - function checks for lowercase characters
 * Return: (0)
 */
int _islower(int c)
{
int a;
c = 0;
if (a >= 97)
return (1);
else if (a <= 122)
return (1);   
else
return (0);
}
